
declare type Matrix = string[][];

declare type Dir = {
    dy: number,
    dx: number,
};

declare type Position = {
    y: number,
    x: number,
};

declare type Num = {
    val: number,
    positions: Position[],
};
